services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  postgres:
    image: pgvector/pgvector:pg18-trixie
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
